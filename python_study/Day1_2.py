a = 10
b = 3

print(f"a + b = {a+b} (덧셈)")  # 덧셈
print(f"a - b = {a-b} (뺄셈)")  # 뺄셈
print(f"a * b = {a*b} (곱셈)")  # 곱셈
print(f"a / b = {a/b} (나눗셈)")  # 나눗셈 (결과는 실수)
print(f"a // b = {a//b} (나눗셈)")  # 나눗셈 (결과는 정수)
print(f"a % b = {a%b} (나머지)")  # 나머지
print(f"a ** b = {a**b} (거듭제곱)")  # 거듭제곱

